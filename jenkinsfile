pipeline {
    agent any

    stages {
        stage('Checkout') {
            steps {
                checkout(
                    [
                        $class: 'GitSCM',
                        branches: [[name: 'main']],
                        userRemoteConfigs: [[credentialsId: 'jenkins-token', url: 'https://github.com/Nimraaa18/jenkins-repo.git']]
                    ]
                )
            }
        }

        stage('Build') {
            steps {
                echo 'Building the application...'
                // Add your build commands here
                // For example: sh 'mvn clean install'
            }
        }

        stage('Test') {
            steps {
                echo 'Running unit tests...'
                // Add your test commands here
                // For example: sh 'mvn test'
            }
        }

        stage('Deploy to Testing Environment') {
            steps {
                echo 'Deploying to testing environment...'
                // Add your deployment commands here
                // For example: sh 'kubectl apply -f testing-deployment.yaml'
            }
        }

        stage('Deliver to Production') {
            when {
                expression { currentBuild.resultIsBetterOrEqualTo('SUCCESS') }
            }
            steps {
                echo 'Delivering to production environment...'
                // Add your deployment commands here
                // For example: sh 'kubectl apply -f production-deployment.yaml'
            }
        }
    }

    post {
        always {
            script {
                echo 'Pipeline finished!'
            }
        }
    }
}
